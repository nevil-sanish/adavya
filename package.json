{
  "name": "adavya",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm ci --prefix apps/website && npm ci --prefix apps/mobile && npm ci --prefix apps/api && npm ci --prefix firebase",
    "dev:website": "npm run dev --prefix apps/website",
    "dev:mobile": "npm run dev --prefix apps/mobile",
    "dev:api": "npm run dev --prefix apps/api",
    "emulators": "npm run emulators --prefix firebase",
    "build": "npm run build --prefix apps/website && npm run build --prefix apps/mobile && npm run build --prefix apps/api",
    "test": "npm run test:shared && npm run test:rules && npm run test:api",
    "test:shared": "node --test packages/shared/tests/",
    "test:rules": "npm test --prefix firebase",
    "test:api": "npm run test:emulator --prefix apps/api"
  }
}
