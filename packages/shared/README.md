# Shared logic

Reserved for types, validation, and API contracts used by multiple apps.
Extract code here when a task needs it in more than one app, with explicit package exports and dependencies at that time.
Keep browser APIs and server credentials out of this directory.
